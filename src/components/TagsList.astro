---
import Badge from './Badge.astro'

interface Props {
  tags: string[]
}

const { tags } = Astro.props
---

<div class='flex flex-wrap self-start gap-2'>
  {tags.slice(0, 2).map(tag => <Badge tag={tag} />)}
</div>
